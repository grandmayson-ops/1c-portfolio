# [Оперативный учет FIFO/LIFO в двух валютах] 🚀

## 📖 О проекте
> В данной конфигурации реализован оперативный учёт товаров, складской учёт товаров не ведется, поступление товаров отражается документом ПриходнаяНакладная, а реализация товаров отражается документом РасходнаяНакладная. При поступлении и реализации, в документах указывается курс валюты(ДолларСША) для ведения учёта в двух валютах. Списание себестоимости, при реализации товаров, определяется в зависимости от выбранного метода списания себестоимости на дату документа - FIFO или LIFO.

## 🛠 Используемые объекты метаданных
- Справочники (Валюты, Номенклатура)
- Документы (Приходная и расходная накладная, проведение документов, движения)
- Перечисления (Учетная политика, FIFO/LIFO)
- Регистры сведений (Учетная политика, Курсы валют)
- Регистры накопления (Остатки номенклатуры для учета количества и себестоимости, Продажи для учета выручки и прибыли)

## Ключевые механизмы
- ✅ Управляемые блокировки — исключительная блокировка регистра остатков при проведении расхода (гарантия корректных остатков при параллельной работе).
- ✅ Временные таблицы и индексация — пакетный запрос с помещением ТЧ документа во временную таблицу и индексацией по номенклатуре (ускорение соединения с остатками).
- ✅ Динамическое изменение текста запроса — подмена сортировки (УБЫВ / ВОЗР) в зависимости от метода списания FIFO/LIFO.
- ✅ Расчёт себестоимости в двух валютах — пропорциональное распределение сумм партии при частичном списании.
- ✅ Контроль остатков перед списанием — проверка доступного количества с выдачей понятного сообщения об ошибке.

## ✨ Функциональные возможности
- Оприходование товаров с помощью документа ПриходнаяНакладная, при проведении создает движения по регистру накопления ОстаткиНоменклатуры, записывает количество и себестоимость товаров, а также партию.
- Продажа(реализация) товаров с помошью документа РасходнаяНакладная, при проведении создает движения по регистру ОстаткиНоменклатуры, проводит контроль остатков и списывает товары по партиям, в зависимости от выбранного метода списания. Создает движения по регистру Продажи, для ведения учета продаж, выручки и прибыли.

## ⚠️ Сложности и их решение 
1. Проблема копеек
- При списании части партии нужно пропорционально разделить себестоимость партии в рублях и долларах. Прямая формула СебестоимостьВРублях = СебестоимостьВРубляхОстаток * Количество / КоличествоОстаток работает, но из-за особенностей двоичной арифметики после многократных частичных списаний остаток партии может составить не 0, а 0,00000001 копейки. Регистр накполения не закрывается в 0. Для решения этой проблемы было использовано так называемое условие последнего списания. Если мы списываем всё без остатка, тогда мы списываем всю накопленную себестоимость, а если списываем какую-то часть, тогда мы рассчитываем себестоимость по формуле. 

2. Отрицательные остатки при параллельной работе пользователей
- Если два менеджера одновременно проводят расходные накладные по одному и тому же товару, оба документа видят одинаковые остатки до записи движений. Возникает расхождение — списывается больше, чем есть. Внедрил управляемую блокировку регистра «ОстаткиНоменклатуры» в исключительном режиме. Блокировка накладывается на записи, соответствующие номенклатуре из табличной части документа, до чтения остатков. Второй документ ожидает снятия блокировки первым и читает уже актуальные остатки. Отрицательные остатки исключены.

3. Производительность при большом количестве партий
Если товар имеет тысячи мелких партий (например, кондитерские изделия с разными сроками годности), первоначальная версия алгоритма (построчный обход в цикле с отдельными запросами) работала 5–7 секунд. Пользователи не готовы ждать. Решается созданием временной таблицы с агрегированными данными из ТЧ документа. Затем присоединяются к ней остатки регистра одним пакетным запросом. Индексируется временная таблица по полю «Номенклатура». В цикле обход группировки по номенклатуре, а внутри — детальные записи остатков. Такой подход позволил сократить количество обращений к базе до одного-двух запросов.

4. Динамический выбор FIFO/LIFO в одном запросе
Метод списания выбирается на дату документа, но текст запроса должен сортировать партии по-разному: для FIFO — по возрастанию даты/времени партии, для LIFO — по убыванию. В тексте запроса жёстко прописана сортировка УБЫВ (для LIFO по умолчанию). Если в учётной политике выбран FIFO, я делаю строковую замену УБЫВ → ВОЗР прямо в коде перед выполнением запроса. Гибкость без дублирования кода. Легко добавлять новые методы сортировки.

## 🚀 Быстрый старт
1. Создайте новую информационную базу 1С.
2. В конфигураторе выберите "Конфигурация -> Загрузить конфигурацию из файлов" и укажите папку `tasks/*проект*/src/Configuration`(для загрузки в формате XML).
3. В конфигураторе выберите "Администрирование -> Загрузить информационную базу" и укажите папку `tasks/*проект*/dist/*ФАЙЛ .dt*`(для загрузки в формате .dt).
4. Запустите в режиме предприятия.
При выгрузке в формате XML, документы и прочие объекты, которые были созданы в режиме предприятия, не выгружаются, их необходимо создать самостоятельно для проверки или
загрузить информационную базу в формате .dt, где уже созданы объекты для демонстрации.

## 📬 Контакты
- Telegram: @vladimir12390
- Email: grandmayson@gmail.com
- GitHub: github.com/grandmayson-ops