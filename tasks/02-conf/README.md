# [Оперативный учет FIFO/LIFO в двух валютах] 🚀

## 📖 О проекте
> Конфигурация реализует оперативный учет номенклатуры (оборудования) с контролем сроков годности и эксплуатации.
Поступление оборудования отражается документом ПриходнаяНакладная с указанием срока годности в днях. ВводВЭксплуатацию списывает оборудование со склада по принципу FIFO по сроку годности (сначала самое «старое»), рассчитывает срок эксплуатации и переносит в регистр эксплуатации. ВыбытиеОборудования автоматически списывает просроченное оборудование как по сроку годности (со склада), так и по срокам годности/эксплуатации (из эксплуатации).

## 🛠 Используемые объекты метаданных
- Справочник Номенклатура (реквизит СрокЭксплуатацииДней)
- Документы:ПриходнаяНакладная (табличная часть СписокНоменклатуры с полями Количество, Сумма, СрокГодностиДней), ВводВЭксплуатацию, ВыбытиеОборудования
- Регистры накопления: ОстаткиНоменклатуры (измерения: Номенклатура, СрокГодности; ресурсы: Количество, Себестоимость), ОборудованиеВЭксплуатации (измерения: Номенклатура, СрокГодности, СрокЭксплуатации; ресурсы: Количество, Себестоимость)

## Ключевые механизмы
- ✅ Преобразование дней в дату срока годности с помощью функции ДОБАВИТЬКДАТЕ()
- ✅ Управляемые блокировки в исключительном режиме для предотвращения параллельного списания одного и того же оборудования
- ✅ Временные таблицы и индексация табличной части для ускорения соединения с остатками
- ✅ Приоритетное списание по сроку годности (FIFO) — сортировка остатков по возрастанию срока годности
- ✅ Условие «последнего списания» для точного обнуления себестоимости при полном списании
- ✅ Пакетный запрос к двум регистрам для автоматического списания просроченного оборудования

## ✨ Функциональные возможности
- Оприходование оборудования с автоматическим расчетом даты срока годности на основе количества дней от даты документа.

- Ввод оборудования в эксплуатацию:
- Контроль достаточности остатков с понятными сообщениями об ошибках.
- Списание по FIFO по сроку годности (сначала самое скоропортящееся).
- Автоматический расчет даты окончания срока эксплуатации.
- Перенос в регистр ОборудованиеВЭксплуатации.

- Выбытие (списание) просроченного оборудования:
- Со склада — по истекшему сроку годности
- Из эксплуатации — по истекшему сроку годности или сроку эксплуатации

## ⚠️ Сложности и их решение 
1. Проблема копеек при частичном списании
- При пропорциональном расчете себестоимости остаток мог не обнуляться из-за погрешностей двоичной арифметики.
Решение: условие последнего списания — если списывается весь остаток, берётся вся себестоимость, иначе — пропорциональный расчет.

2. Параллельный ввод в эксплуатацию одного оборудования разными пользователями
- Два документа могли одновременно «увидеть» одни и те же остатки и списать больше, чем есть.
Решение: исключительная управляемая блокировка остатков по номенклатуре и диапазону сроков годности до чтения остатков.

3. Корректное списание просроченного оборудования
- Нужно одновременно обработать просрочку на складе и в эксплуатации, исключив гонки данных.
Решение: блокировка обоих регистров по диапазонам дат (до начала дня документа) и пакетный запрос для получения всех просроченных остатков.

## 🚀 Быстрый старт
1. Создайте новую информационную базу 1С.
2. В конфигураторе выберите "Конфигурация -> Загрузить конфигурацию из файлов" и укажите папку `tasks/*проект*/src/Configuration`(для загрузки в формате XML).
3. В конфигураторе выберите "Администрирование -> Загрузить информационную базу" и укажите папку `tasks/*проект*/dist/*ФАЙЛ .dt*`(для загрузки в формате .dt).
4. Запустите в режиме предприятия.
При выгрузке в формате XML, документы и прочие объекты, которые были созданы в режиме предприятия, не выгружаются, их необходимо создать самостоятельно для проверки или
загрузить информационную базу в формате .dt, где уже созданы объекты для демонстрации.

## 📬 Контакты
- Telegram: @vladimir12390
- Email: grandmayson@gmail.com
- GitHub: github.com/grandmayson-ops